# Blacklist the playstation driver for Brook PS5 controller boards
# This prevents the generic playstation driver from binding to Brook devices
# Copy this file to /etc/modprobe.d/ to activate

# Completely blacklist the playstation driver (uncomment if needed)
# blacklist playstation

# Alternative: Use install directive to prevent automatic loading
# install playstation /bin/false

# Note: After copying this file to /etc/modprobe.d/, run:
# sudo depmod -a
# sudo update-initramfs -u